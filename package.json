{
    "scripts": {
      "build": "wasm-pack build --target web",
      "pages": "npm run build && cargo run && gh-pages --dist build --dest . --repo https://github.com/zzd/gen-brand-photo-pictrue"
    },
    "devDependencies": {
      "gh-pages": "^4.1.8"
    }
  }